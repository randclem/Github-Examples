name: example-workflow
on: [push]
jobs:
  hello-world:
    if: github.repository == 'randclem/Github-Examples'
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World"
        run:  echo "Hello World!"
  goodbye-moon:
    if: github.repository == 'octocat/octo-kit'
    runs-on: ubuntu-latest
    steps:
      - name: "Goodbye Moon"
        run:  echo "Goodbye Moon!"